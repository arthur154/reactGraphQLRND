type Address {
  street: String!
  suite: String
  city: String!
  zipcode: String!
}

type User {
  id: ID!
  name: String!
  username: String!
  email: String!
  phone: String
  address: Address
}

type Query {
  user(id: ID!): User
  users: [User]!
}
